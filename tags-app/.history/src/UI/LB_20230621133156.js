export default function Leaderboard() {

    const array = [
        {
            user : "kevin",
            rank : 3,
            key : 1
        },
        {
            user : "kevin 2",
            rank : 5,
            ke7 : 2
        }
    ]

    return (
        <div>
            {array.map((x) => )}
        </div>
    )
}